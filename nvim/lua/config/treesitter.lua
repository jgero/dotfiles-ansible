require'nvim-treesitter.configs'.setup {
    ensure_installed = { 'dart', 'latex', 'svelte', 'html', 'css' },
    indent = {
      enable = true
    },
    highlight = {
        enable = true,
    },
}

